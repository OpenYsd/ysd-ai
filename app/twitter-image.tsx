/**
 * بطاقة X/Twitter — **نفس الصورة**، لا نسخة ثانية.
 *
 * الأبعاد المطلوبة واحدة (1200×630 لبطاقة `summary_large_image`)، ونسختان
 * تعنيان تصميمين يفترقان يوم يُعدَّل أحدهما.
 *
 * و`runtime` **لا يُعاد تصديره**: Next لا يتعرّف على إعداد مقطعٍ يصل عبر
 * إعادة تصدير، فيحذّر عند كل بناء ويستعمل الافتراضي على أي حال.
 */
export { default, alt, size, contentType } from "./opengraph-image";
