/**
 * إصدار الوثائق القانونية — **تمثيلٌ واحد في التطبيق** (v0.9.16، 6E).
 *
 * ── لماذا ثابتٌ في الشيفرة ومصدرٌ في القاعدة معًا ──
 *
 * `platform_settings.terms_version` هو **مصدر الحقيقة وقت التشغيل**: به
 * تُقارَن صفوف `user_consents`، وبه تُسجَّل الموافقات الجديدة. لا يُنازعه
 * هذا الملفّ.
 *
 * وما يفعله هذا الملفّ شيءٌ آخر: يجعل **النسخة المعروضة** في الوثائق وفي
 * شاشة القبول قيمةً واحدة بدل ثلاثة نصوصٍ متفرّقة. وكانت ثلاثة فعلًا —
 * في `privacy` و`terms` وفي الترحيل — فيسهل أن يُبدَّل أحدها ويبقى الباقي
 * يعرض تاريخًا مضى.
 *
 * ── والانحراف يُمسَك ──
 *
 * `tests/v128` يقرأ الترحيل ويطابق قيمته بهذا الثابت. فلو رُفع أحدهما دون
 * الآخر لَسقط الاختبار — بدل أن تعرض الوثيقة نسخةً والقاعدة تفرض غيرها.
 *
 * ── وما لا يفعله ──
 *
 * لا يُسجّل موافقة، ولا يُعدّل صفًّا قائمًا، ولا يمسّ إذن التدريب. قبولُ
 * الشروط والإذن بالتدريب نظامان منفصلان تمامًا.
 */

/**
 * إصدار الحزمة القانونية الحالي.
 *
 * رُفع في المرحلة 6E لأن سياسة الخصوصية تغيّرت **جوهريًّا** في 6A: صارت
 * تُفصح عن المساهمة الاختيارية في تحسين YSD. ومن وافق على نصّ يوليو وافق
 * على وثيقةٍ لا تذكرها.
 */
export const LEGAL_BUNDLE_VERSION = "2026-08-21";

/** ما قبله — يُذكر كي يكون شرطُ التبديل في الترحيل مقروءًا */
export const LEGAL_PREVIOUS_VERSION = "2026-07-15";

/** الترحيل الذي يرفع الإعداد — يقرأه الاختبار ليمسك الانحراف */
export const LEGAL_VERSION_MIGRATION = "0046_legal_bundle_2026_08_21.sql";
