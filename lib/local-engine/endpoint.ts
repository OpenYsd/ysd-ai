/**
 * عنوانُ المحرّك المحلّيّ — مصدرٌ واحد لكلّ ميزةٍ تتّصل به.
 *
 * ── لماذا نُقل إلى هنا ──
 *
 * كان يعيش في `lib/local-image/flag.ts` لأنّ الصورةَ كانت أوّلَ من احتاجه.
 * والآن يحتاجه الاقترانُ أيضًا، وسيحتاجه الصوت. ونسخُ الرقم في كلّ ميزةٍ
 * يعني أنّ تغييرَه يومًا يُصلح بعضَها ويكسر الباقي صامتًا: الواجهةُ تنادي
 * منفذًا وسياسةُ الأمن تفتح آخر.
 *
 * ★ ولم يتغيّر شيءٌ في سلوك الصورة: ملفُّها يُعيد التصدير، فكلُّ ما كان
 *   يستورد منه يستورد القيمةَ نفسَها.
 *
 * ── ولماذا منفذٌ واحد لا مدى ──
 *
 * لو تنقّل المحرّكُ بين المنافذ لاحتاجت `connect-src` أن تكتب
 * `127.0.0.1:*` — وذلك يُصرّح لكلِّ خدمةٍ تعمل على جهاز المستخدم، لا
 * لمحرّكنا وحدَه. فالتثبيتُ هنا هو شرطُ أن تبقى السياسةُ ضيّقة.
 *
 * وعند التزاحم على المنفذ يمتنع المحرّكُ ويُعلن، ولا يقفز إلى غيره.
 */

/** المنفذُ المثبَّت للمحرّك المحلّيّ */
export const LOCAL_ENGINE_PORT = 47615;

/**
 * الأصلُ الوحيد المسموح به في السياسة.
 *
 * ★ `127.0.0.1` لا `localhost`: الاسمُ يُحلّ في بعض البيئات إلى `::1`
 *   أو إلى غير الحلقة المحلّية أصلًا، والعنوانُ الرقميّ لا يُحلّ.
 */
export const LOCAL_ENGINE_ORIGIN = `http://127.0.0.1:${LOCAL_ENGINE_PORT}`;
