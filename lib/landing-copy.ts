/**
 * نصوص البيانات الوصفية (v0.9.13، المرحلة 6B) — **للآلات لا للواجهة**.
 *
 * ── لماذا ليست في `lib/i18n.tsx` ──
 *
 * تلك الوحدة `"use client"` وتقرأ سياق React، و`app/layout.tsx` يصدّر
 * `metadata` على الخادم قبل أي سياق. فاستيرادُها هناك خطأُ بناء.
 *
 * ── وما يُكتب هنا وما لا يُكتب ──
 *
 * وصفٌ واحد يقرأه محرّك البحث ومُعاينُ الروابط. ونصوصُ الواجهة كلُّها في
 * `lib/i18n.tsx` كما هي — فلا مسارَ ترجمةٍ ثانٍ.
 *
 * والوصف يقول ما يفعله المنتج فعلًا: محادثة، وملفات، ومشاريع. ولا يَعِد
 * بذكاءٍ خارقٍ ولا بدقّةٍ مضمونة — فما يُكتب هنا يُقتبس في مواضع لا نملكها.
 */

export const LANDING_DESCRIPTION =
  "YSD AI — مساحة عمل ذكية للمحادثة والعمل على الملفات وتنظيم المشاريع. عربية أولًا، مع دعم الإنجليزية.";

/** السطر البصريّ للبطاقة الاجتماعية — يبقى إنجليزيًّا في الصورة المولَّدة */
export const LANDING_HEADLINE_EN = ["Think Deeper.", "Build Better."] as const;
