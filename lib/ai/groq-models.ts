/**
 * سلسلة نماذج Groq — **مستقلة تمامًا** عن `FREE_MODEL_CHAIN`.
 *
 * الفصل مقصود: تلك سلسلة OpenRouter بمهلها وتهدئتها وبوابة سبرها، وهذه
 * سلسلة مزوّد آخر بمفتاح آخر وحدود أخرى. خلطهما كان سيجعل `fallback_count`
 * يعني شيئين في وقت واحد — وهو ما أضاع تشخيص ثلاث حوادث سابقة.
 */
export const GROQ_MODEL_CHAIN: readonly string[] = [
  "openai/gpt-oss-120b", // الأقوى — يُجرَّب أولًا
  "openai/gpt-oss-20b", // احتياط أخفّ
];

/**
 * المعرّف الداخلي لمزوّد Groq في المسار.
 *
 * لا يُعرض للمستخدم ولا يدخل قائمة الاختيار: Groq **احتياط** لا خيار.
 */
export const GROQ_PROVIDER_ID = "groq";
