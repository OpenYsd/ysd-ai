# خارطة طريق YSD AI

الحالة: 🟢 مكتمل · 🟡 قيد العمل · ⚪ لم يبدأ

> **v0.3.0** (2026-07-13): RAG محلي مجاني مكتمل ومُختبر E2E من المتصفح
> (PDF متعدد الصفحات + DOCX): تقسيم، embeddings محلية 384، استرجاع آمن،
> أرقام صفحات، حماية Prompt Injection، عتبة مُعايَرة (migration 0007).
> **Production Hardening** (2026-07-14): طابور RAG دائم في قاعدة البيانات
> (rag_jobs مصدر الحقيقة)، claim ذري (SKIP LOCKED)، استئناف بعد التوقف،
> retry بـ backoff، عزل RLS، تنظيف — اختبار ضغط 20/20 + rag-check 22/22.
> المعالجة request-driven؛ Worker مستقل موثّق كمتطلب service role (غير مُفعّل).
>
> **v0.2.0** (2026-07-13): نظام الملفات مكتمل ومُختبر 36/36 — رفع وتخزين خاص
> واستخراج نص وحدود متوافقة مع بيئة التخزين الفعلية (migrations 0005+0006).
>
> **v0.1.1** (2026-07-13): Regression نهائي 23/23 + منصة 54/54 — بث حقيقي مجاني
> عبر OpenRouter بنماذج معتمدة عربيًا، Language Guard، تسجيل النموذج الفعلي،
> حد يومي مُفعّل ومُختبر، والمشاريع كاملة.
>
> **v0.1.0** (2026-07-12): المرحلتان 0 و1 مكتملتان ومختبرتان — مصادقة كاملة،
> محادثة حقيقية بالبث والحفظ، RLS مُثبت بين مستخدمين، واجهة منسقة كمبيوتر/جوال.
>
> ⚠️ قاعدة تشغيل: لا تشغّل `npm run build` أثناء عمل `npm run dev` (يتشاركان `.next`).

## المرحلة 0 — الأساس
- 🟢 بنية المشروع (Next.js · TS Strict · Tailwind)
- 🟢 طبقة AIProviderAdapter + موفر Anthropic
- 🟢 مخطط قاعدة البيانات + RLS (migration 0001)
- 🟢 مسار /api/chat آمن مع Streaming وتسجيل الاستهلاك
- 🟢 الوسيط الأمني وحماية /admin
- 🟢 التوثيق الأولي

## المرحلة 1 — MVP
- 🟢 واجهة المحادثة الكاملة: بث SSE، إيقاف التوليد، إعادة توليد، تعديل رسالة المستخدم، Markdown + كتل كود مع نسخ، اختيار النموذج
- 🟢 المصادقة الكاملة: دخول، تسجيل مع تأكيد بريد، نسيان/إعادة تعيين كلمة المرور، خروج، حماية الصفحات
- 🟢 حفظ واسترجاع المحادثات من قاعدة البيانات (إنشاء/حذف ناعم/إعادة تسمية/عنوان تلقائي/بحث)
- 🟢 الهيكل: شريط جانبي قابل للطي، متجاوب للجوال، وضع داكن/فاتح، عربي RTL/إنجليزي LTR
- 🟢 صفحة الحساب: الملف الشخصي (الاسم) + الباقة + عداد الاستهلاك الشهري الحقيقي (رسائل + Tokens)
- 🟢 صفحة الإعدادات: المظهر، اللغة، النموذج الافتراضي (تُحفظ في user_preferences)
- 🟢 migration 0002: سياسات تعديل الرسائل وتسجيل الاستهلاك
- 🟢 قاعدة البيانات مطبّقة على Supabase (16 جدولًا + 23 سياسة + trigger + seed) — تم التحقق سلوكيًا
- 🟢 اختبارات Runtime حقيقية (scripts/runtime-check.mjs): 38/38 — تسجيل، trigger الملف الشخصي، جلسات، CRUD محادثات ورسائل، عزل RLS بين مستخدمين، IDOR عبر API، رسالة خطأ عربية عند نفاد رصيد الموفر دون تسريب أسرار
- 🟢 بث Streaming حقيقي عبر OpenRouter المجاني — مُختبر من المتصفح (إرسال + إعادة توليد + حفظ)
- 🟢 جودة اللغة: Allowlist نماذج مجانية مُتحقق منها بالعربية + Language Guard (نافذة فحص + إعادة محاولة بنموذج احتياطي) + اختبارات آلية 12 + اختبار حي 4/4
- 🟢 التقاط النموذج الفعلي من OpenRouter وتسجيله في messages/usage_events وعرضه في وضع التطوير
- 🟡 بث Anthropic — بانتظار إضافة رصيد API (معالجة الخطأ مُختبرة ومؤكدة)
- ⚪ صورة الملف الشخصي وحذف الحساب

## المرحلة 2 — الملفات
- 🟢 رفع PDF/DOCX/TXT/MD/PNG/JPG/WEBP بتحقق مزدوج (امتداد+MIME) وتعقيم أسماء ومنع traversal
- 🟢 تخزين خاص (Bucket غير عام) بمسار userId/… وسياسات Storage + Signed URLs
- 🟢 استخراج نص حقيقي (unpdf/mammoth/ترميز عربي) وحالات uploaded→processing→ready/failed مع سبب الفشل
- 🟢 حدود مركزية لكل باقة (حجم/عدد/مساحة) مع سقف مزود التخزين 50MB: min(plan, provider)
- 🟢 واجهة كاملة: سحب وإفلات، تقدم حقيقي مع إلغاء، بحث/تصفية، ربط بمشاريع/محادثات، إعادة معالجة
- 🟢 اختبارات: 21 وحدة + 36 Runtime (منها 8 عزل RLS/Storage) — مُختبر رفع فعلي من المتصفح
- 🟢 RAG محلي مجاني (migration 0007): pgvector + تقسيم عربي/إنجليزي + embeddings محلية (multilingual-e5-small, 384) + استرجاع آمن بـ RPC + حقن سياق مُسوَّر + مصادر تحت الرد
- 🟢 عتبة مُعايَرة على قياس فعلي (أرضية 0.78 + ثقة 0.80) وتصريح «لم أجد» عند عدم التطابق
- 🟢 اختبارات: 9 تقسيم + embeddings حية + rag-check 22/22 (استرجاع، عزل، Prompt Injection، حذف المقاطع)

## المرحلة 3 — المشاريع (v0.2.0 قيد الإعداد)
- 🟢 API كامل: إنشاء/قراءة/تعديل/حذف ناعم مع Zod + ملكية على الخادم + RLS
- 🟢 الاسم والوصف والتعليمات الخاصة (تدخل موجه النظام في كل محادثة بالمشروع) والنموذج الافتراضي
- 🟢 صفحة /projects: بحث، فرز (نشاط/اسم)، عدادات محادثات وملفات، آخر نشاط، حالات فراغ/خطأ/تحميل
- 🟢 صفحة /projects/[id]: تعديل، حذف، محادثة جديدة داخل المشروع، ربط/فك ربط محادثات
- 🟢 اختبارات Runtime: 54/54 — تشمل 5 اختبارات IDOR/RLS خاصة بالمشاريع
- ⚪ ربط الملفات بالمشاريع (مع المرحلة 2 — الملفات)

## المرحلة 4 — الإدارة والباقات
- ⚪ لوحة إدارة: مستخدمون، استهلاك، نماذج، حدود، سجل عمليات
- ⚪ صفحة الباقات (Free/Plus/Pro/Business) — بدون دفع حقيقي بعد
- ⚪ الدفع (Stripe أو مزود محلي) — بعد اكتمال الأساسيات

## المرحلة 5 — الجودة
- ⚪ اختبارات Unit للتحقق والحدود
- ⚪ اختبار E2E: دخول → محادثة → رسالة → رد متدفق → إعادة تحميل → بقاء المحادثة → مشروع
- ⚪ تحسين الأداء: Virtualization، Pagination، تحسين الصور
